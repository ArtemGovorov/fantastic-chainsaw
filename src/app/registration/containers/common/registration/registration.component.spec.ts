import { RegistrationComponent } from './registration.component';
import {} from 'jasmine';

describe('Reg Test', () => {
  
  it('should be true', () => {
    expect(true).toEqual(true);
    // const component = new RegistrationComponent(null, null, null, null, null);
  });
      
});